# Blood Pressure Log — v3
# Static site, no build step. Supabase is called directly from the browser.

[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index-v3.html"
  status = 200
