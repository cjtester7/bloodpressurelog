# Blood Pressure Log — v4
# Static site, no build step. Supabase is called directly from the browser.

[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index-v4.html"
  status = 200
